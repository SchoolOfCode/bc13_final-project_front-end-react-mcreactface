// This is not working

/* import { useSupabaseClient } from "@supabase/auth-helpers-react"

const supabase = useSupabaseClient()

export default async function getMusicians() {
    let { data: musicians, error } = await supabase
        .from("musicians")
        .select("*")

    return musicians
}
 */
